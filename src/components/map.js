import React from 'react';
import styled from 'styled-components';

const Map = styled.iframe`
  height: 90vh;
  overflow-x: hidden;
  border: none;
  position: absolute;
  width: 100%;
`;

export default Map;